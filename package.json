{
  "name": "localci",
  "version": "0.0.1",
  "private": false,
  "type": "module",
  "description": "to be added",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "clean": "pnpm -r --no-bail --if-present run clean",
    "gen": "pnpm -r --if-present run gen",
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test",
    "lint": "pnpm -r --if-present run lint",
    "typecheck": "pnpm -r --if-present run typecheck",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "spell": "cspell \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "prepare": "husky",
    "pipeline": "export PATH=\"$HOME/.cargo/bin:$HOME/go/bin:$PATH\" && node blueprints/ci.js",
    "ci": "pnpm run pipeline",
    "docs:build": "pnpm -C docs-site build"
  },
  "keywords": [
    "electron",
    "typescript"
  ],
  "author": "Michael Rieck (Michael--) <mr@number10.de>",
  "license": "MIT",
  "engines": {
    "node": ">=20.19.0",
    "pnpm": ">=10.28.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "@commitlint/cli": "^20.3.1",
    "@commitlint/config-conventional": "^20.3.1",
    "@eslint/js": "^9.39.2",
    "@typescript-eslint/eslint-plugin": "^8.53.1",
    "@typescript-eslint/parser": "^8.53.1",
    "cspell": "^9.6.0",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "globals": "^17.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.0",
    "ts-proto": "^2.11.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.17"
  }
}
